# ~/.zshrc（GNU Stow で管理）
# Interactive-shellの設定

[[ -o interactive ]] || return

# alias and functions
for f in "$HOME/.config/zsh/aliases.zsh" "$HOME/.config/zsh/functions.zsh"; do
  [[ -f "$f" ]] && source "$f"
done

# fzf の設定
if command -v fzf >/dev/null 2>&1; then
  source <(fzf --zsh)
fi

# 端末ごとの上書き設定可能にする
[[ -f "$HOME/.zshrc.local" ]] && source "$HOME/.zshrc.local"
